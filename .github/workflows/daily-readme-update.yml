name: Daily Readme Update

on:
  workflow_dispatch:
  schedule:
    - cron: '0 0 * * *'

permissions:
  contents: write
  issues: write

jobs:
  new-trivia:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      - name: Import, decrypt, and decompress DB
        env:
          PYTHONPATH: src
          TRIVIA_DB_PASSWORD: ${{ secrets.TRIVIA_DB_PASSWORD }}
          TRIVIA_DB_SALT: ${{ secrets.TRIVIA_DB_SALT }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_USERNAME: ${{ github.repository_owner }}
          GITHUB_REPO: ${{ github.event.repository.name }}
        run: python manage.py import-db
      - name: Run new trivia job
        env:
          PYTHONPATH: src
          TRIVIA_DB_PASSWORD: ${{ secrets.TRIVIA_DB_PASSWORD }}
          TRIVIA_DB_SALT: ${{ secrets.TRIVIA_DB_SALT }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_USERNAME: ${{ github.repository_owner }}
          GITHUB_REPO: ${{ github.event.repository.name }}
        run: python manage.py new-trivia
      - name: Update DB (write changes)
        id: update_db
        env:
          PYTHONPATH: src
          TRIVIA_DB_PASSWORD: ${{ secrets.TRIVIA_DB_PASSWORD }}
          TRIVIA_DB_SALT: ${{ secrets.TRIVIA_DB_SALT }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_USERNAME: ${{ github.repository_owner }}
          GITHUB_REPO: ${{ github.event.repository.name }}
        run: python manage.py update-db
      - name: Export, encrypt, and compress DB
        if: steps.update_db.outputs.db_changed == 'true'
        env:
          PYTHONPATH: src
          TRIVIA_DB_PASSWORD: ${{ secrets.TRIVIA_DB_PASSWORD }}
          TRIVIA_DB_SALT: ${{ secrets.TRIVIA_DB_SALT }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_USERNAME: ${{ github.repository_owner }}
          GITHUB_REPO: ${{ github.event.repository.name }}
        run: python manage.py export-db
      - name: Upload DB artifact
        if: steps.update_db.outputs.db_changed == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: db-artifact-new-trivia
          path: src/data/trivia_database.db.gz

  new-fact:
    needs: new-trivia
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      - name: Download DB artifact
        uses: actions/download-artifact@v4
        with:
          name: db-artifact-new-trivia
          path: src/data/
      - name: Import, decrypt, and decompress DB
        env:
          PYTHONPATH: src
          TRIVIA_DB_PASSWORD: ${{ secrets.TRIVIA_DB_PASSWORD }}
          TRIVIA_DB_SALT: ${{ secrets.TRIVIA_DB_SALT }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_USERNAME: ${{ github.repository_owner }}
          GITHUB_REPO: ${{ github.event.repository.name }}
        run: python manage.py import-db
      - name: Run new fact job
        env:
          PYTHONPATH: src
          TRIVIA_DB_PASSWORD: ${{ secrets.TRIVIA_DB_PASSWORD }}
          TRIVIA_DB_SALT: ${{ secrets.TRIVIA_DB_SALT }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_USERNAME: ${{ github.repository_owner }}
          GITHUB_REPO: ${{ github.event.repository.name }}
        run: python manage.py new-fact
      - name: Update DB (write changes)
        id: update_db
        env:
          PYTHONPATH: src
          TRIVIA_DB_PASSWORD: ${{ secrets.TRIVIA_DB_PASSWORD }}
          TRIVIA_DB_SALT: ${{ secrets.TRIVIA_DB_SALT }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_USERNAME: ${{ github.repository_owner }}
          GITHUB_REPO: ${{ github.event.repository.name }}
        run: python manage.py update-db
      - name: Export, encrypt, and compress DB
        if: steps.update_db.outputs.db_changed == 'true'
        env:
          PYTHONPATH: src
          TRIVIA_DB_PASSWORD: ${{ secrets.TRIVIA_DB_PASSWORD }}
          TRIVIA_DB_SALT: ${{ secrets.TRIVIA_DB_SALT }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_USERNAME: ${{ github.repository_owner }}
          GITHUB_REPO: ${{ github.event.repository.name }}
        run: python manage.py export-db
      - name: Upload DB artifact
        if: steps.update_db.outputs.db_changed == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: db-artifact-new-fact
          path: src/data/trivia_database.db.gz

  process-answers:
    needs: new-fact
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      - name: Download DB artifact
        uses: actions/download-artifact@v4
        with:
          name: db-artifact-new-fact
          path: src/data/
      - name: Import, decrypt, and decompress DB
        env:
          PYTHONPATH: src
          TRIVIA_DB_PASSWORD: ${{ secrets.TRIVIA_DB_PASSWORD }}
          TRIVIA_DB_SALT: ${{ secrets.TRIVIA_DB_SALT }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_USERNAME: ${{ github.repository_owner }}
          GITHUB_REPO: ${{ github.event.repository.name }}
        run: python manage.py import-db
      - name: Run process answers job
        env:
          PYTHONPATH: src
          TRIVIA_DB_PASSWORD: ${{ secrets.TRIVIA_DB_PASSWORD }}
          TRIVIA_DB_SALT: ${{ secrets.TRIVIA_DB_SALT }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_USERNAME: ${{ github.repository_owner }}
          GITHUB_REPO: ${{ github.event.repository.name }}
        run: python manage.py process-answers
      - name: Update DB (write changes)
        id: update_db
        env:
          PYTHONPATH: src
          TRIVIA_DB_PASSWORD: ${{ secrets.TRIVIA_DB_PASSWORD }}
          TRIVIA_DB_SALT: ${{ secrets.TRIVIA_DB_SALT }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_USERNAME: ${{ github.repository_owner }}
          GITHUB_REPO: ${{ github.event.repository.name }}
        run: python manage.py update-db
      - name: Export, encrypt, and compress DB
        if: steps.update_db.outputs.db_changed == 'true'
        env:
          PYTHONPATH: src
          TRIVIA_DB_PASSWORD: ${{ secrets.TRIVIA_DB_PASSWORD }}
          TRIVIA_DB_SALT: ${{ secrets.TRIVIA_DB_SALT }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_USERNAME: ${{ github.repository_owner }}
          GITHUB_REPO: ${{ github.event.repository.name }}
        run: python manage.py export-db
      - name: Upload DB artifact
        if: steps.update_db.outputs.db_changed == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: db-artifact-process-answers
          path: src/data/trivia_database.db.gz

  update-readme:
    needs: process-answers
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      - name: Try download DB artifact from process-answers
        id: download_db_process_answers
        continue-on-error: true
        uses: actions/download-artifact@v4
        with:
          name: db-artifact-process-answers
          path: src/data/
      - name: Try download DB artifact from new-fact
        if: steps.download_db_process_answers.outcome != 'success'
        id: download_db_new_fact
        continue-on-error: true
        uses: actions/download-artifact@v4
        with:
          name: db-artifact-new-fact
          path: src/data/
      - name: Try download DB artifact from new-trivia
        if: steps.download_db_process_answers.outcome != 'success' && steps.download_db_new_fact.outcome != 'success'
        uses: actions/download-artifact@v4
        with:
          name: db-artifact-new-trivia
          path: src/data/
      - name: Import, decrypt, and decompress DB
        env:
          PYTHONPATH: src
          TRIVIA_DB_PASSWORD: ${{ secrets.TRIVIA_DB_PASSWORD }}
          TRIVIA_DB_SALT: ${{ secrets.TRIVIA_DB_SALT }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_USERNAME: ${{ github.repository_owner }}
          GITHUB_REPO: ${{ github.event.repository.name }}
        run: python manage.py import-db
      - name: Run update readme job
        env:
          PYTHONPATH: src
          TRIVIA_DB_PASSWORD: ${{ secrets.TRIVIA_DB_PASSWORD }}
          TRIVIA_DB_SALT: ${{ secrets.TRIVIA_DB_SALT }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_USERNAME: ${{ github.repository_owner }}
          GITHUB_REPO: ${{ github.event.repository.name }}
        run: python manage.py update-readme
      - name: Upload README artifact
        uses: actions/upload-artifact@v4
        with:
          name: readme-artifact
          path: README.md

  commit-and-push:
    needs: update-readme
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      - name: Download README artifact
        uses: actions/download-artifact@v4
        with:
          name: readme-artifact
          path: .
      - name: Download DB artifact
        uses: actions/download-artifact@v4
        with:
          name: db-artifact-update-readme
          path: src/data/
      - name: Commit and push changes
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add src/data/trivia_database.db.gz README.md
          git diff --quiet && git diff --staged --quiet || git commit -m "🤖 Daily update: Trivia, Fact, Leaderboard"
          git push origin main 